<Window x:Class="KursApp.clientsList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursApp"
        mc:Ignorable="d"
        Title="clientsList" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="70,44,0,0">
            <TextBox x:Name="SearchBoxClient" Width="200" Margin="0,0,10,0" />
            <Button Content="Пошук" Width="75" Click="SearchButtonClient"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="385,44,0,0">
            <TextBox x:Name="SearchBoxRNOKPP" Width="170" Margin="0,0,10,0" />
            <Button Content="Пошук" Width="75" Click="SearchBoxRNKOPP"/>
        </StackPanel>
        <Button Content="Back" FontSize="14" HorizontalAlignment="Left" Margin="713,32,0,0" VerticalAlignment="Top"
                Height="23" Width="53" Click="Button_Click"/>
        <DataGrid x:Name="ClientsGrid" AutoGenerateColumns="False" CanUserAddRows="False" Margin="48,199,48,26">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ПІБ" Binding="{Binding FullName}" />
                <DataGridTextColumn Header="Дата народження" Binding="{Binding DateOfBirth, StringFormat=d}" />
                <DataGridTextColumn Header="РНОКПП" Binding="{Binding Rnokpp}" />
                <DataGridTextColumn Header="Контактні данні" Binding="{Binding ContactData}" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock FontSize="14" HorizontalAlignment="Left" Margin="70,19,0,0" TextWrapping="Wrap" Text="Сортування по ПІБ" VerticalAlignment="Top" Height="25" Width="127"/>
        <TextBlock FontSize="14" HorizontalAlignment="Left" Margin="386,19,0,0" TextWrapping="Wrap" Text="Сортування по РНОКПП" VerticalAlignment="Top" Height="25" Width="161"/>
        <Button Content="Очистити фільтри" HorizontalAlignment="Left" Margin="70,95,0,0" VerticalAlignment="Top" Width="114" Click="Clear_filters"/>
    </Grid>
</Window>
