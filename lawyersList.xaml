<Window x:Class="KursApp.lawyersList" Background="#F8F9FA"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursApp"
        mc:Ignorable="d"
        Title="lawyersList" Height="450" Width="800">
    <Grid>
        <Button Content="Back" FontSize="14" HorizontalAlignment="Left" Margin="713,32,0,0" VerticalAlignment="Top" Height="23" Width="53" Click="Button_Click"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="70,44,0,0">
            <TextBox x:Name="SearchBox" Width="170" Margin="0,0,10,0" />
            <Button Content="Пошук" Width="75" Click="SearchButton_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="364,44,0,0">
            <TextBox x:Name="SearchBoxService" Width="170" Margin="0,0,10,0" />
            <Button Content="Пошук" Width="75" Click="SearchButtonService"/>
        </StackPanel>
        <Button Content="Очистити фільтри" HorizontalAlignment="Left" Margin="70,95,0,0" VerticalAlignment="Top" Width="114" Click="Clear_filters"/>
        <DataGrid x:Name="LawyersGrid" AutoGenerateColumns="False" CanUserAddRows="False" Margin="48,199,48,26" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="ПІБ" Binding="{Binding FullName}" />
                <DataGridTextColumn Header="Досвід" Binding="{Binding Experience}" />
                <DataGridTextColumn Header="Ліцензія" Binding="{Binding License}" />
                <DataGridTextColumn Header="Контактні данні" Binding="{Binding ContactData}" />
                <DataGridTextColumn Header="Спеціалізація" Binding="{Binding TypeOfCase}" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock FontSize="14" HorizontalAlignment="Left" Margin="70,19,0,0" TextWrapping="Wrap" Text="Сортування по ПІБ" VerticalAlignment="Top" Height="25" Width="127"/>
        <TextBlock FontSize="14" HorizontalAlignment="Left" Margin="356,19,0,0" TextWrapping="Wrap" Text="Сортування за типом справи" VerticalAlignment="Top" Height="25" Width="186"/>
    </Grid>
</Window>
