<Window x:Class="KursApp.service"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursApp"
        mc:Ignorable="d"
        Title="service" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="70,44,0,0">
            <TextBox x:Name="SearchBoxClient" Width="170" Margin="0,0,10,0" />
            <Button Content="Пошук" Width="75" Click="SearchBoxClient_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="385,44,0,0">
            <TextBox x:Name="SearchBoxLawyer" Width="170" Margin="0,0,10,0" />
            <Button Content="Пошук" Width="75" Click="SearchBoxLawyer_Click"/>
        </StackPanel>
        <Button Content="Back" FontSize="14" HorizontalAlignment="Left" Margin="713,32,0,0" VerticalAlignment="Top" Height="23" Width="53" Click="Button_Click"/>
        <Button Content="Очистити фільтри" HorizontalAlignment="Left" Margin="209,86,0,0" VerticalAlignment="Top" Width="116" Click="Clear_filters" Height="22"/>
        <DataGrid x:Name="SalesGrid"  AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,199,0,26">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Клієнт" Binding="{Binding Clients}" />
                <DataGridTextColumn Header="Адвокат" Binding="{Binding Lawyers}" />
                <DataGridTextColumn Header="Відкриття справи" Binding="{Binding SaleDate, StringFormat=d}" />
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" />
                <DataGridTextColumn Header="Закриття справи" Binding="{Binding EndDate, StringFormat=d}" />
                <DataGridTextColumn Header="Тип справи" Binding="{Binding TypeOfCase}" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock FontSize="14" HorizontalAlignment="Left" Margin="70,19,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="25" Width="159">
            <Run Text="Сортування по "/><Run Language="uk-ua" Text="клієнту"/>
        </TextBlock>
        <TextBlock FontSize="14" HorizontalAlignment="Left" Margin="385,19,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="25" Width="159"
                   ><Run Text="Сортування по "/><Run Language="uk-ua" Text="адвокату"/>
        </TextBlock>
        <Button x:Name="Close" Content="Закрити справу" HorizontalAlignment="Left" Margin="70,86,0,0" VerticalAlignment="Top" Height="22" Width="98" Click="Close_Click"/>
    </Grid>
</Window>
